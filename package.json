{
  "name": "databowl-webhook",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "sync": "node scripts/sync.js --since $(date +%F)"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.46.1",
    "node-fetch": "^3.3.2"
  }
}
